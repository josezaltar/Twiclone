# === Segurança / Django ===
# Gere uma chave nova para uso real (não use valores deste .example)
DJANGO_SECRET_KEY=COLOQUE_SUA_CHAVE_AQUI
# Em DEV pode ser True; em produção (PythonAnywhere) use False
DJANGO_DEBUG=True

# Hosts permitidos — inclua seu domínio do PythonAnywhere em produção
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Confiança de CSRF — inclua seus domínios com esquema (https://)
# Em produção, adicione: https://SEUUSUARIO.pythonanywhere.com e seu domínio do Netlify
DJANGO_CSRF_TRUSTED=http://localhost:3000

# === CORS (origens permitidas) ===
# Se o front (CRA/Netlify) chamar a API diretamente, inclua o domínio aqui.
# Em DEV: http://localhost:3000
# Em produção (Netlify): https://SEUSITE.netlify.app
CORS_ALLOWED_ORIGINS=http://localhost:3000

# === Banco de Dados ===
# Se não definir, o settings usa: sqlite:///<BASE_DIR>/db.sqlite3 (recomendado em DEV)
# Exemplos:
# - SQLite relativo ao projeto (funciona em qualquer SO):
# DATABASE_URL=sqlite:///db.sqlite3
# - PythonAnywhere (caminho absoluto; ajuste SEUUSUARIO):
# DATABASE_URL=sqlite:////home/SEUUSUARIO/Twiclone/api/db.sqlite3
